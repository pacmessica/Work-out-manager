<<<<<<< HEAD

<div class="workout-body">
  <h1 class="workout-title"><%= @workout.name %></h1>
  <p class="workout-description"><%= @workout.description %></p>

  <div class="exercise-container">
    <% @workout.exercises.each_with_index do |exercise, index| %>
    <div class="exercise-panel">
      <%= index + 1 %>.
      <%= exercise.name %>
      <div class="exercise-image"><%= image_tag(exercise.image, width:"100", height:"auto") %></div>
    </div>
    <% end %>
  </div>

  <a href="#" class="button">Start Workout</a>

</div>
||||||| merged common ancestors
<h1>Workouts#show</h1>
<p>Find me in app/views/workouts/show.html.erb</p>
=======
<h1><%= @workout.name %></h1>
<h2><%= @workout.description %>


<% @workout.exercises.each_with_index do |exercise, index| %>
<div class="exercise-panel">
  <%= index + 1 %>.
  <%= exercise.name %>
  <div class="exercise-image"><%= image_tag(exercise.image) %></div>
</div>
<% end %>
>>>>>>> add workouts show page
